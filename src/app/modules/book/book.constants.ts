export const bookSearchableFields = ['title', 'author', 'genre'];

export const bookFilterableFields = ['category'];

export const bookFilterMap: Record<string, string> = {
  category: 'categoryId',
};
